package controllers;

import javafx.application.Application;
import javafx.stage.Stage;

public class GraphScreen extends Application {
	@Override
	public void start(Stage stage) {

	}
	/*

	 Button plot = new Button("Plot");
        plot.disableProperty().bind(Bindings.or(Bindings.isEmpty(
            title.textProperty()), Bindings.isEmpty(text.textProperty())));
        plot.setOnAction(e -> {
            lineChart = new LineChart<>(xAxis, yAxis);
            lineChart.setLegendVisible(false);
            main.setCenter(lineChart);
            lineChart.setTitle("Frequency of words in " + title.getText());
            lineChart.getData().add(count(title.getText(), text.getText()));
        });

        xAxis = new CategoryAxis();
        yAxis = new NumberAxis();
        yAxis.setLabel("Frequency");
        xAxis.setLabel("Word");
        lineChart = new LineChart<>(xAxis, yAxis);


	 */
}
